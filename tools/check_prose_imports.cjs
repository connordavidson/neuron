// Audit all private files using the established checksum-pinned validator, then gate prose only.
const fs=require('node:fs');const {spawnSync}=require('node:child_process');const [manifest,output]=process.argv.slice(2);if(!manifest||!output)throw Error('Expected MANIFEST OUTPUT');const run=spawnSync(process.execPath,[require.resolve('./validate_local_imports.cjs'),manifest,output],{stdio:'inherit'});if(run.error)throw run.error;if(!fs.existsSync(output))process.exit(run.status||1);const {results}=JSON.parse(fs.readFileSync(output));const prose=results.filter(r=>r.slug!=='changing-world-order');if(prose.length!==6||prose.some(r=>!r.passed))process.exitCode=1;
